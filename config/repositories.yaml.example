# 管理対象リポジトリの設定
#
# 各リポジトリに適用するワークフローを指定します。
# ワークフロー名は templates/ 配下のファイル名（.yml を除く）と対応します。
#
# このファイルを config/repositories.yaml にコピーして使用してください。

repositories:
  # 例: owner/repository-name の形式で指定
  # - name: kspace-trk/example-repo
  #   workflows:
  #     - ci           # templates/ci.yml を配布
  #     - release      # templates/release.yml を配布
  #   branch: main     # オプション: デフォルトブランチ（デフォルト: main）
  #   runsOn: arc-runner-set  # オプション: 使用するrunner（未設定の場合はubuntu-latestが使用されます）

  # Gemini workflows を使用する例
  # - name: kspace-trk/example-repo-with-gemini
  #   workflows:
  #     - ci                        # CI/CD ワークフロー
  #     - gemini-dispatch           # @gemini-cli コメントで呼び出し
  #   runsOn: arc-runner-set       # self-hosted runnerを使用

  # ここに管理対象リポジトリを追加してください
